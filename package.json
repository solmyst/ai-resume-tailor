{
  "name": "vite-react-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run install-backend && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "backend": "cd backend && (venv/bin/python app.py || venv/bin/python3 app.py || venv/Scripts/python app.py || python3 app.py || python app.py)",
    "setup-backend": "cd backend && (venv/bin/python setup.py || venv/bin/python3 setup.py || venv/Scripts/python setup.py || python3 setup.py || python setup.py)",
    "start": "cd backend && (venv/bin/python app.py || venv/bin/python3 app.py || venv/Scripts/python app.py || python3 app.py || python app.py)",
    "start:dev": "concurrently \"npm run dev\" \"npm run backend\"",
    "render-build": "npm run install-backend",
    "install-backend": "cd backend && (python3 -m venv venv || python -m venv venv || echo 'No venv support') && (venv/bin/pip install -r requirements.txt || venv/bin/pip3 install -r requirements.txt || venv\\Scripts\\pip install -r requirements.txt || pip install -r requirements.txt || pip3 install -r requirements.txt || echo 'Warning: Flask requirements could not be installed')"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "jspdf": "^3.0.4",
    "lucide-react": "^0.344.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.18",
    "eslint": "^9.9.1",
    "eslint-plugin-react-hooks": "^5.1.0-rc.0",
    "eslint-plugin-react-refresh": "^0.4.11",
    "gh-pages": "^6.3.0",
    "globals": "^15.9.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.5.3",
    "typescript-eslint": "^8.3.0",
    "vite": "^5.4.2"
  }
}
